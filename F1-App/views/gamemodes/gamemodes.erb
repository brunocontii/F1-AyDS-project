<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <script src="https://kit.fontawesome.com/a2dd6045c4.js" crossorigin="anonymous"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu</title>
    <link rel="icon" href="icon-1-50.png">
    <link rel="stylesheet" href="styleMenu.css">
    <style>
    .message {
        font-size: 2.5rem;
        text-align: center;
        margin: 20px 0;
        padding: 20px;
        background: rgba(0, 0, 0, 0.8);
        border-radius: 10px;
        color: #ffcc00;
        text-shadow: 2px 2px 4px #000;
        display: inline-block;
    }
    </style>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
    $(document).ready(function() {
        function updateLives() {
            $.get('/gamemodes', function(data) {
                console.log(data);
                $('#lives-count').text(data.lives);
            });
        }

        setInterval(updateLives, 20000);

        updateLives();
    });
    </script>
</head>
<body>
    <header>
        <div class="left">
            <a href="/how-to-play" class="menu-item" data-lang-key="howToPlay">How To Play</a>
        </div>
        <div class="right">
            <div class="user-info">
                <div class="currency">
                    <img src="coin.png" alt="Monedas" class="icon">
                    <span><%=@current_user ? @current_user.cant_coins : 0%></span>
                </div>
                <div class="lives">
                    <img src="heart.png" alt="Vidas" class="icon">
                    <span id = "lives-count"><%=@current_user ? @current_user.cant_life : 0%></span>
                </div>
                <div class="profile">
                    <img src="pilot.png" alt="Profile" class="profile-pic" onclick="toggleProfileMenu()">
                    <div id="profile-menu" class="profile-menu">
                        <a href="/profile" data-lang-key="profile">Profile</a>
                        <div class="change-language" onclick="toggleLanguage()">
                            <span data-lang-key="changeLanguage">Change Language</span>
                            <img src="argentina.png" alt="Idioma" class="language-flag">
                        </div>
                        <a href="/logout" data-lang-key="logout">Log out</a>
                    </div>
                </div>
            </div>
        </div>
    </header>
    <main>
        <% if session[:message] %>
            <div class="message" style="color: <%= session[:color] %>;">
                <%= session[:message] %>
            </div>
            <script>
                setTimeout(function() {
                    window.location.href = '<%= @redirect_url || "/gamemodes" %>';
                }, 4000); 
            </script>
            <% session[:message] = nil %>
            <% session[:color] = nil %>
        <% end %>
        <h1 data-lang-key="gamemodes">Gamemodes</h1>
        <div class="gamemodes">
            <a href="/gamemodes/progressive" class="gamemode" data-lang-key="progressive">Progressive</a>
            <a href="/gamemodes/free" class="gamemode" data-lang-key="free">Free</a>
        </div>
    </main>
    <script>
        const translations = {
            en: {
                howToPlay: 'How To Play',
                profile: 'Profile',
                changeLanguage: 'Change Language',
                logout: 'Log out',
                gamemodes: 'Gamemodes',
                progressive: 'Progressive',
                free: 'Free'
            },
            es: {
                howToPlay: 'Cómo Jugar',
                profile: 'Perfil',
                changeLanguage: 'Cambiar Idioma',
                logout: 'Cerrar sesión',
                gamemodes: 'Modos de Juego',
                progressive: 'Progresivo',
                free: 'Libre'
            }
        };

        function toggleProfileMenu() {
            var menu = document.getElementById('profile-menu');
            menu.style.display = menu.style.display === 'block' ? 'none' : 'block';
        }

        function toggleLanguage() {
            var currentFlag = document.querySelector('.language-flag');
            var newLang = currentFlag.src.includes('argentina.png') ? 'en' : 'es';
            var newFlag = newLang === 'en' ? 'usa.png' : 'argentina.png';
            currentFlag.src = newFlag;

            // Update text content according to selected language
            updateLanguage(newLang);
        }

        function updateLanguage(lang) {
            const elements = document.querySelectorAll('[data-lang-key]');
            elements.forEach(el => {
                const key = el.getAttribute('data-lang-key');
                el.textContent = translations[lang][key];
            });
        }

        // Initialize language based on current flag
        document.addEventListener('DOMContentLoaded', () => {
            const currentFlag = document.querySelector('.language-flag').src;
            const initialLang = currentFlag.includes('argentina.png') ? 'es' : 'en';
            updateLanguage(initialLang);
        });
    </script>
</body>
</html>
